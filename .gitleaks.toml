title = "gitleaks config"

[allowlist]
description = "Allowlist for false positives in AWS SDK example files and dependency bundles"
paths = [
  ".*/layers/deps/",
  ".*/agent_files/deployment_package/",
  ".*/\\.venv/",
  "cdk.out/",
  "\\.env\\.example$",
]
